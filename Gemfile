source "https://rubygems.org"

# The below causes jekyll to run in safe mode, causing plugins to be ignored. See https://github.com/jekyll/jekyll/issues/5265
gem 'github-pages', group: :jekyll_plugins

# gem 'sanskrit-documentation-theme'
# gem 'jekyll-regex-replace'
